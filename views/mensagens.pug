doctype html
html(lang="pt-BR")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Mensagens
    body

        header
        
        section
            if mensagens.length
                for mensagem in mensagens
                    h2 
                        a(href=`/Administrador/:${admin}/mensagem/:${mensagem._id.toString()}`)= mensagem.Assunto
            else 
                p Não existe nenhuma mensagem no sistema!
